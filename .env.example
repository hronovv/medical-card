# Copy to .env
PORT=8080
APP_ENV=local

# Docker Compose: DB_HOST = postgres (service name)
# Local go run (make dev-api): DB_HOST=localhost
DB_HOST=postgres
DB_PORT=5432
DB_NAME=medical_card
DB_USER=medical
DB_PASSWORD=password1234
DB_SCHEMA=public
